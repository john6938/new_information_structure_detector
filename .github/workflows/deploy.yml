name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: info-structure-client/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: info-structure-client

      - name: Build
        run: npm run build
        working-directory: info-structure-client
        env:
          BASE_URL: /new_information_structure_app/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: info-structure-client/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
